---
Description: The border-image property allows you to use an image instead of a standard border style.
---

# border-image

The **`border-image`** CSS property allows you to draw an image around an element's border. This replaces the standard solid or dashed lines.

## Syntax

```css
border-image: source slice width outset repeat;
