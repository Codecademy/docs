# justify-content

The `justify-content` property is used in CSS Flexbox to control how flex items are aligned and distributed along the main axis of a flex container when extra space is available.

---

## Syntax

```css
justify-content: flex-start | flex-end | center | space-between | space-around | space-evenly;
