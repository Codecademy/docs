# Python Built-In Functions: `float()`

---
Title: "Python Built-In Functions: `float()`" # Note: the file name should be the same as the title, but lowercase, with dashes instead of spaces, and all punctuation removed
Subjects: # Please only use Subjects in the subjects.md file (github.com/Codecademy/codepedia-content/blob/main/documentation/subjects.md). If that list feels insufficient, feel free to create a new Subject and add it to subjects.md in your PR!
  - "language fluency"
  - "code foundations"
Tags: # Please only use Tags in the tags.md file (github.com/Codecademy/codepedia-content/blob/main/documentation/tags.md). If that list feels insufficient, feel free to create a new Tag and add it to tags.md in your PR!
  - "function"
  - "methods"
  - "strings"
Catalog Content: # Please use course/path landing pages, rather than linking to individual content items. If listing multiple URLs, please put the most relevant one first
  - "https://www.codecademy.com/learn/learn-python-3"
  - "https://www.codecademy.com/learn/paths/analyze-data-with-python"
---
## Definition
***
The built-in `float()` takes in a numeric, string, or no value and a copy of that value in the floating point data type.

## Syntax
***
```py
float()
```

## Example 1
***
Use `float()` to change the datatype of an integer value `314` to float:
```codebyte/python
print(float(314))
```

## Example 2
***
Use `float()` to change the datatype of a string value `"314"` to float:
```codebyte/python
print(float("314"))
```

## Example 3
***
Use `float()` to create a new float value:
```codebyte/python
f = float()
print(f)
```
